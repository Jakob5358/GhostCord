{
  "name": "@ghostcord/utilities",
  "version": "1.0.0",
  "description": "The utilities module for ghost cord.",
  "main": "dist/root.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc:init": "tsc --init",
    "watch": "tsc -b src -w",
    "build": "tsc -p tsconfig.json",
    "format": "prettier --write {src,scripts}/**/*.ts"
  },
  "keywords": [],
  "author": "ThatGuyJamal#2659",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^17.0.30",
    "prettier": "^2.6.2",
    "typescript": "^4.6.4"
  }
}
